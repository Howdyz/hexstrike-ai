# HexStrike AI — Windows launcher build dependencies
#
# Used by .github/workflows/build-windows.yml to build HexStrikeAI-Windows.exe.
# As of the WSL2 rewrite, this launcher is a thin orchestrator — it never
# imports hexstrike_server.py or any of its dependencies (flask, mitmproxy,
# selenium, ...) directly. Those all live inside the WSL2 rootfs image
# (wsl/Dockerfile, built by build-wsl-rootfs.yml), a real Kali userland where
# they install via apt/pip the normal way. The launcher only needs `requests`
# to download that image, and tkinter for the GUI (stdlib, no wheel needed).

requests>=2.31.0,<3.0.0
pyinstaller>=6.0.0
